// Arrays and Loops
//Step 1
numberList = [];

//Step 2 - Add numbers from 0 to 5 into the array numberList and print them
for (i = 0; i <= 5; i++)
    numberList.push(i);
console.log(numberList);

// Step 3 Remove the last item in the array and print the new array
numberList.pop(i);
console.log(numberList);